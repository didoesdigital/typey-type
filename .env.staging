# Since any variables exposed to your Vite source code will end up in your client bundle, VITE_* variables should not contain any sensitive information <https://vite.dev/guide/env-and-mode#env-files>

# Google Analytics ID for development, staging, and QA
VITE_GA_MEASUREMENT_ID="G-45R7RGF4FZ"

# Vite options used in config
GENERATE_SOURCEMAP=true